let num = 3

let vereficador = (num >= 1 && num <=5) ? 'batata' :
                    (num >= 6 && num <= 10) ? 'banana' :
                    (num >= 11 && num <=20) ?   'baixo' :
                    'NAO SEI' 

console.log(vereficador)